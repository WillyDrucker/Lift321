// ==========================================================================
// REACT NATIVE CONFIGURATION
//
// Configures React Native CLI and Metro bundler behavior.
// Defines asset paths for font linking via react-native-asset.
//
// Dependencies: None
// Used by: React Native CLI, Metro bundler
// ==========================================================================

module.exports = {
  project: {
    ios: {},
    android: {},
  },
  // Font assets linked to native projects
  assets: ['./src/assets/fonts'],
};
